# Voxmire Mobile

Future placeholder.

Mobile may use Flutter or another native-oriented stack later. This folder exists only to reserve the app boundary. Shared behavior should be captured through contracts, schemas, export formats, and product rules in `packages/*`.
